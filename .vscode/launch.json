{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "mycDebug",
            "program": "${workspaceFolder}/a.o",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build with Makefile"
        }
    ]
  }